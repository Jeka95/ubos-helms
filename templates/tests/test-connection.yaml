apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ubos-nodered.fullname" . }}-test-connection"
  labels:
    {{- include "ubos-nodered.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ubos-nodered.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
