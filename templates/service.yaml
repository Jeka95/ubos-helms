apiVersion: v1
kind: Service
metadata:
  name: {{ include "ubos-nodered.fullname" . }}
  labels:
    {{- include "ubos-nodered.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ubos-nodered.selectorLabels" . | nindent 4 }}
