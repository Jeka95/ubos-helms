{{- if .Values.auth.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "swagger-ui.fullname" . }}-generate-auth
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: htpasswd-generator
          image: lachlanevenson/k8s-kubectl:v1.30.0
          command: ["/bin/sh", "-c"]
          args:
            - |
              echo "🔐 Creating htpasswd for user '{{ .Values.auth.username }}'"
              apk add --no-cache apache2-utils
              htpasswd -nb {{ .Values.auth.username }} {{ .Values.auth.password }} > /auth/auth
              echo "📦 Creating secret '{{ include "swagger-ui.fullname" . }}-basic-auth'"
              kubectl create secret generic {{ include "swagger-ui.fullname" . }}-basic-auth \
                --from-file=auth=/auth/auth \
                -n {{ .Release.Namespace }} \
                --dry-run=client -o yaml | kubectl apply -f -
          volumeMounts:
            - name: auth-volume
              mountPath: /auth
      volumes:
        - name: auth-volume
          emptyDir: {}
{{- end }}
